import { Loader, Loading } from "../styled/Loading";

const LoaderComponent = () => {
    return (
        <Loading>
            <Loader />
        </Loading>
    );
}

export default LoaderComponent;